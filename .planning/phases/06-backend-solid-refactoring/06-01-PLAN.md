---
phase: 06-backend-solid-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/Program.cs
  - backend/Services/ISm2Engine.cs
  - backend/Services/Sm2Engine.cs
  - backend/Endpoints/EndpointExtensions.cs
autonomous: true
requirements:
  - SOLID-01
must_haves:
  truths:
    - "Backend compiles successfully with zero errors"
    - "All Minimal API endpoints respond correctly"
    - "SM-2 calculation runs through an isolated class decoupled from Program.cs"
  artifacts:
    - path: "backend/Services/ISm2Engine.cs"
      provides: "SM-2 Engine contract"
    - path: "backend/Services/Sm2Engine.cs"
      provides: "SM-2 calculation implementation"
    - path: "backend/Endpoints/EndpointExtensions.cs"
      provides: "Modular API routes mapping"
  key_links:
    - from: "backend/Program.cs"
      to: "backend/Endpoints/EndpointExtensions.cs"
      via: "app.MapNexTiEndpoints(key)"
    - from: "backend/Endpoints/EndpointExtensions.cs"
      to: "backend/Services/ISm2Engine.cs"
      via: "ISm2Engine injection in Route Delegate"
---

<objective>
Refatorar o backend C# aplicando as diretrizes SOLID (SRP e DIP) para isolar o motor de cálculo SM-2 e modularizar os endpoints HTTP que hoje poluem o arquivo Program.cs.

Purpose: Melhorar a manutenibilidade, testabilidade e separação de responsabilidades do sistema Nex_TI.
Output: Serviço ISm2Engine/Sm2Engine e classe EndpointExtensions criada, com Program.cs reduzido e limpo.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar o Contrato e Implementação do Motor SM-2 (SRP/DIP)</name>
  <files>backend/Services/ISm2Engine.cs, backend/Services/Sm2Engine.cs</files>
  <action>
    Criar a interface ISm2Engine em backend/Services/ contendo a assinatura do método Calcular.
    Criar a classe Sm2Engine implementando a lógica matemática do SM-2 (repetições, fator facilidade, cálculo do próximo intervalo de dias).
  </action>
  <verify>
    <automated>Arquivos existem e contêm as declarações de classe e interface na namespace NexTI_API.Services</automated>
  </verify>
  <done>ISm2Engine e Sm2Engine criados com sucesso.</done>
</task>

<task type="auto">
  <name>Task 2: Criar Modularização das Rotas da API (SRP)</name>
  <files>backend/Endpoints/EndpointExtensions.cs</files>
  <action>
    Criar a classe EndpointExtensions em backend/Endpoints/ para conter o mapeamento de todas as rotas de Minimal API.
    Ajustar o endpoint do SM-2 (/api/progresso/atualizar) para receber ISm2Engine via Injeção de Dependência e usá-lo para processar as regras matemáticas.
  </action>
  <verify>
    <automated>EndpointExtensions.cs contém as declarações de rota e os records DTO da API</automated>
  </verify>
  <done>Rotas movidas e rota do SM-2 atualizada para usar injeção de dependência.</done>
</task>

<task type="auto">
  <name>Task 3: Simplificar Program.cs e Registrar Dependências</name>
  <files>backend/Program.cs</files>
  <action>
    Remover todos os mapeamentos de rotas inline de Program.cs.
    Registrar ISm2Engine como Singleton: builder.Services.AddSingleton&lt;ISm2Engine, Sm2Engine&gt;().
    Acionar as extensões de rotas no pipeline de execução usando app.MapNexTiEndpoints(key).
  </action>
  <verify>
    <automated>Program.cs contém apenas configurações e chamada de injeção/rotas extensas</automated>
  </verify>
  <done>Program.cs limpo e dependências devidamente injetadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | A validação do ID do usuário autenticado no token JWT é mantida na rota modularizada, impedindo IDOR e requisições não autorizadas |
</threat_model>

<success_criteria>
- Backend compila com sucesso via dotnet build
- Os testes manuais de integração de rotas e cálculo de progresso via SM-2 funcionam normalmente
</success_criteria>
