---
phase: 07-bugfixes-and-robustness
plan: 01
type: execute
wave: 1
depends_on: [06-backend-solid-refactoring]
files_modified:
  - database/NexTI_DB.sql
  - backend/Endpoints/EndpointExtensions.cs
  - pages/dashboard/js/app.js
autonomous: true
requirements:
  - ROBUSTNESS-01
must_haves:
  truths:
    - "Database is seeded with ENADE and simulated exam data"
    - "Backend parses User ID securely using TryParse"
    - "Frontend dashboard uses local storage as a fallback when API responds empty"
  artifacts:
    - path: "database/NexTI_DB.sql"
      provides: "DML queries and seeds for simulated exams"
    - path: "backend/Endpoints/EndpointExtensions.cs"
      provides: "Secure claim parsing validation"
    - path: "pages/dashboard/js/app.js"
      provides: "Resilient API-to-local fallback"
---

<objective>
Corrigir falhas de segurança e robustez no ecossistema Nex_TI:
1. Inserir sementes de dados (Seed) para simulados do ENADE e UML no banco SQL Server.
2. Tratar de forma defensiva a leitura das claims no backend para evitar quebra em rotas autenticadas.
3. Criar fallback robusto de dados locais no frontend se a API de flashcards retornar listas vazias.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@database/NexTI_DB.sql
@backend/Endpoints/EndpointExtensions.cs
@pages/dashboard/js/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed das Tabelas de Simulados e Provas</name>
  <files>database/NexTI_DB.sql</files>
  <action>
    Adicionar os registros de áreas de conhecimento, provas, questões e alternativas do ENADE no final de NexTI_DB.sql.
    Derrubar e recriar o banco SQL Server para aplicar o seed completo.
  </action>
  <verify>
    <automated>Consultar tb_questoes e validar que existem 6 questões cadastradas.</automated>
  </verify>
  <done>Banco populado com 116 flashcards e 6 questões do ENADE estruturadas.</done>
</task>

<task type="auto">
  <name>Task 2: Validação Defensiva de Claims no Backend</name>
  <files>backend/Endpoints/EndpointExtensions.cs</files>
  <action>
    Substituir int.Parse por int.TryParse nos endpoints JWT protegidos.
  </action>
  <verify>
    <automated>dotnet build compila com sucesso.</automated>
  </verify>
  <done>Uso de TryParse implementado de forma segura.</done>
</task>

<task type="auto">
  <name>Task 3: Contingência de Dados no Frontend</name>
  <files>pages/dashboard/js/app.js</files>
  <action>
    Adicionar bloco else de fallback local para o caso da API de flashcards retornar um array vazio.
  </action>
  <verify>
    <automated>O dashboard renderiza as cartas locais se a API de flashcards responder vazia.</automated>
  </verify>
  <done>Fallback implementado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
* A leitura e conversão de identificadores de claims agora é validada defensivamente, impossibilitando que requisições forjadas quebrem a execução do runtime HTTP.
</threat_model>

<success_criteria>
- Backend compila perfeitamente.
- Todos os endpoints de simulados retornam a lista semeada.
</success_criteria>
